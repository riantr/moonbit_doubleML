v0.53.0: global-review fixes + whitebox-test convention + CI loop tightening

Bumps moon.mod version from 0.52.0 to 0.53.0 and inserts a
new CHANGELOG section covering the 5 follow-up commits that
landed since v0.52.0:

  aec880d global-review: 4 critical workspace / CI-cleanliness bugs
  3965c11 global-review: tighten CI Python loop + sync counts in docs
  8b040c9 gitignore: cover `.archived_<date>` scratch suffix
  926fd46 kfold: add whitebox tests for cluster-aware fold expansion
  701ee5b matrix: add whitebox tests for hot-path structural contracts

Highlights (full CHANGELOG entry details each):

Added
  - whitebox-test convention (`*_wbtest.mbt`); 11 tests across
    kfold_wbtest.mbt (5) + matrix_wbtest.mbt (6); codified in
    skills/moonbit_doubleML.md

Fixed (CI / housekeeping)
  - examples/consumer_demo registered in moon.work
  - tracked __pycache__/*.pyc removed
  - doc/native/ + doc/.moon-lock + doc/.moon_db gitignored
  - validate_with_python.py renamed to .archived_2026_09_25
  - *.archived_<date> pattern added to .gitignore (18 files)
  - python-cross-check CI loop now asserts PASS instead of
    silently exiting 0

Changed (docs)
  - estimator count 17 → 22 (19 upstream + 3 extras:
    DIDCSBinary, LPLR, PLPR); Models table expanded
  - example count wording unified to "14 dirs in moon.work"
  - lplr entry corrected from DoubleMLLPQ to DoubleMLLPLR
  - did_cs_binary entry corrected to DoubleMLDIDCSBinary

Verified (v0.53.0 release gate)
  - moon check --deny-warn           : exit=0, 0 diagnostic warnings
  - moon test --target native        : 401 / 401 (390 blackbox + 11 wbtest)
  - moon test --target wasm          : 401 / 401
  - moon test --target wasm-gc       : 407 / 407 (lib + 6 doc tutorials)
  - moon test --target js            : 401 / 401
  - examples/fuzz smoke (native)     : 11 / 11 invariant surfaces
  - 23 / 23 Python cross-validators  : PASS, 0 FAIL

Public API: unchanged (CI-cleanliness + tests + docs only).
The v0.52 surface is byte-identical.

Tag-and-publish step:
  git tag v0.53.0
  git push origin master v0.53.0
  git push github master:main v0.53.0

The github tag push triggers .github/workflows/publish.yml,
which runs moon check + moon test on all 4 backends, then
`moon publish` to Mooncakes and `gh release create` on
github.